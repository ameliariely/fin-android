package fin.fin;

import dagger.Module;

/**
 * Rock on
 */

@Module
public class ConnectModule {
        /*
     * provide stuff specific to the fragment here...
     */
    /*
    This particular type of provides method can usually be omitted if it just returns a new instance
    of a class with an empty constructor, dagger can usually figure that out.

     @Provides
     static HelpfulHelper provideHelpfulHelper() {
         return new HelpfulHelper();
     }
     */
}
